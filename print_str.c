#include "main.h"

/**
 * print_string - prints an input string to
 *              the standard ouput (stdout)
 * @s: the input string
 * Return: the number of printed characters
*/
int print_str(const char *s)
{
	return (puts(s));
}
