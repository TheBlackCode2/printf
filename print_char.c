#include "main.h"

/**
 * print_char - prints an input character to
 *              the standard ouput (stdout)
 * @c: the input char
 * Return: 1 on success, otherwise -1
*/
int print_char(char c)
{
	return (put_char(c));
}
